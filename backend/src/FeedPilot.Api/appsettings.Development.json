{
  "Logging": {
    "LogLevel": {
      "Default": "Debug",
      "Microsoft.AspNetCore": "Information"
    }
  },
  "ConnectionStrings": {
    "_comment": "SQLite dev file so restarts don't wipe users, sessions and orders. Blank = in-memory (wiped each run); a Host=/postgres string uses PostgreSQL.",
    "Default": "Data Source=feedpilot-dev.db"
  },
  "Admin": {
    "ApiKey": "dev-admin-key"
  },
  "Payments": {
    "UpiId": "feedpilot@upi",
    "PayeeName": "FeedPilot"
  }
}
